import 'package:flutter/material.dart';

class VerifyYourAccountView extends StatelessWidget {
  const VerifyYourAccountView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column(children: [Text('Verify Your Account')])),
    );
  }
}
